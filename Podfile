# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
target 'MyGalary' do
  use_frameworks!
  pod 'SwiftyVIPER', '~> 1.0'

  target 'MyGalaryTests' do
    inherit! :search_paths
    pod 'SwiftyVIPER', '~> 1.0'
  end

  target 'MyGalaryUITests' do
    inherit! :search_paths
    pod 'SwiftyVIPER', '~> 1.0'
  end
end